{
  "version": 2,
  "builds": [
    {
      "src": "api/entry-server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/api/entry-server.js" }
  ]
}
